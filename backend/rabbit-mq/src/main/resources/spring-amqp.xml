<?xml version="1.0" encoding="utf-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/rabbit
           http://www.springframework.org/schema/rabbit/spring-rabbit-1.5.xsd
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <rabbit:admin connection-factory="connectionFactory"/>

    <rabbit:queue id="testQueue" durable="true"
                  auto-delete="false" exclusive="false" name="testQueue"/>

    <rabbit:direct-exchange name="geex" durable="true"
                            auto-delete="false" id="geexexchage">
        <rabbit:bindings>
            <rabbit:binding queue="testQueue" key="testQueueKey"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>

    <rabbit:listener-container acknowledge="auto" concurrency="2" max-concurrency="8"
                               connection-factory="connectionFactory">
        <rabbit:listener ref="testListener" queue-names="testQueue"/>
    </rabbit:listener-container>

    <bean id="jsonMessageConverter"
          class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter"/>

    <bean id="retryTemplate" class="org.springframework.retry.support.RetryTemplate">
        <property name="backOffPolicy">
            <bean class="org.springframework.retry.backoff.ExponentialBackOffPolicy">
                <property name="initialInterval" value="1000"/>
                <property name="multiplier" value="2"/>
                <property name="maxInterval" value="10000"/>
            </bean>
        </property>
        <property name="retryPolicy">
            <bean class="org.springframework.retry.policy.SimpleRetryPolicy">
                <property name="maxAttempts" value="10"/>
            </bean>
        </property>
    </bean>

    <rabbit:template exchange="geex" id="amqpTemplate" connection-factory="connectionFactory"
                     message-converter="jsonMessageConverter" retry-template="retryTemplate"/>

    <rabbit:connection-factory id="connectionFactory"
                               host="192.168.102.40" username="guest" password="guest" port="5672"
                               requested-heartbeat="10" channel-cache-size="30"/>
</beans>